name: Hello CI

on:
  push:
    # branches:
    #   - hello

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print message
        run: npm run blabla
